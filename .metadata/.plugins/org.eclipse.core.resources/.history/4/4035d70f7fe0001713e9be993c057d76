import java.util.Arrays;
import java.util.Scanner;

public class Main {
	public static void main(String[] args) {
		Scanner sc = new Scanner(System.in);
		int N = sc.nextInt();
		int[] arr = new int[N];
		double sum = 0.0;
		int min = 4000;
		int max = -4000;

		for (int i = 0; i < N; i++) {
			arr[i] = sc.nextInt();
			sum += arr[i];
			if (arr[i] > max)
				max = arr[i];
			if (arr[i] < min)
				min = arr[i];
		}

		Arrays.sort(arr);

		// System.out.format("%.0f%n", (sum / N)); // »ê¼úÆò±Õ
		// System.out.println(arr[(N - 1) / 2]); // Áß¾Ó°ª
		System.out.println(findMany(arr)); // ÃÖºó°ª
		// System.out.println((max - min)); // ¹üÀ§
		sc.close();
	}

	static int findMany(int[] arr) {
		int manyCount = 0; // ÃÖºó°ª °³¼ö
		int[] manyVal = { arr[0], arr[0] }; // ÃÖºó°ª

		printArr(arr);

		for (int i = 0; i < arr.length; i++) {
			int j = i;
			int count = 0;

			while (arr[j] == arr[j + 1]) {
				count++;
				j++;
				if (j >= arr.length - 1)
					break;
			}

			if (count > manyCount) {
				manyCount = count;
				manyVal[0] = arr[i];
			} else if (count == manyCount) {
				if (manyVal[0] > arr[i])
					manyVal[1] = arr[i];
				else {
					manyVal[1] = manyVal[0];
					manyVal[0] = arr[i];
				}

			}
		}

		if (manyVal[1] == arr[0])
			return manyVal[1];
		else
			return manyVal[0];
	}

	static void printArr(int[] arr) {
		for (int i = 0; i < arr.length; i++) {
			System.out.print(arr[i] + " ");
		}
		System.out.println();
	}

}
